services:
  test-runner:
    image: python:3.12-slim
    command: >
      bash -c "pip install pytest -q && pytest -v --tb=short || true"
    environment:
      - CONTROL_PLANE_OPENAI_API_KEY
      - CLI_OPENAI_API_KEY
    volumes:
      - .:/app
    working_dir: /app
